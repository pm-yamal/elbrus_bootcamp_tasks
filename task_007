// Задание 7
// Уследить за всеми приборами и мигающими лампочками очень сложно. Юрию нужна помощь! 
// Напишите функцию, которая принимает arrStr и num, а возвращает новый массив со словами, длина которых равна num.
// ПРИМЕР: filterArray(["Коррекция", "Виток", "Воздух", "Газоанализатор", "Температура"], 6) //"Воздух"

function filterArray(arrStr, num) {
  const arr2 = [];
  for (let i = 0; i < arrStr.length; i++) {
    if (arrStr[i].length === num) {
      arr2.push(arrStr[i]);
    }
  }
  return arr2;
}
console.log(filterArray(['Коррекция', 'Виток', 'Воздух', 'Газоанализатор', 'Температура'], 6));
