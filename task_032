// Задание 32
// Напиши функцию, которая принимает строку и возвращает новую строку, в которой все буквы будут в верхнем регистре.
// Например: "hello" → "HELLO"; "JavaScript" → "JAVASCRIPT".

function capitalizeWord(str) {
  let strNew = '';
  strNew = str.toUpperCase();
  return strNew;
}
console.log(capitalizeWord('hello'));
console.log(capitalizeWord('JavaScript'));
