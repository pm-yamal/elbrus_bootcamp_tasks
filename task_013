// Задание 13
// ЦУП прислал рекомендации по предполетной подготовке, но они очень запутанные: следовать нумерованным шагам надо по особой схеме. Мало того, что нужно какие-то пропускать, так еще и начинать надо не с первого пункта, и заканчивать не последним.
// // Помогите Юрию Гагарину написать функцию, которая принимает три аргумента и возвращает массив целых чисел от min до max с шагом step.
// ПРИМЕР: generateRange(2, 10, 2) //[2, 4, 6, 8, 10]

function generateRange(min, max, step) {
  const arr = [];
  for (let i = min; i <= max; i += step) {
    arr.push(i);
  }
  return arr;
}
console.log(generateRange(2, 10, 2));
